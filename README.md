Install globally:

> npm i -g browserify nodemon parcel typescript uglify-js

Install locally:

> npm i

Run a server:

> nodemon

Run in debug mode:

> npm run dev

Run in release mode:

> npm run release
